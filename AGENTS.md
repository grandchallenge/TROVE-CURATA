# Agent instructions

TROVE–CURATA is the governed GCL data-estate and curation-control repository.

Until `TC-BOOTSTRAP-ACTIVATE-001` records two-sided protected readback, the repository is not active and `TC-FIXTURE-006` may not begin.

Treat `bootstrap/intellect-snapshot/` as an immutable imported historical snapshot. Do not edit its files. Corrections must be prospective records outside the snapshot and must preserve the source identity and historical state.

Providers, classifiers, metrics, thresholds, Passports and manifests do not possess corpus-admission authority. Under `GCL-AGENT-STAFFING-001` version `1.0.0`, one Codex system may implement, staff distinct non-reserved logical audit passes, merge through satisfied protected controls, and read back the result. Authoring-system Adversary and Referee passes are `non_authoring_read_only`. Automation may not exercise Human Steward authority or write directly to protected branches.

Resolve ordinary governance staffing from the effective protected INTELLECT authority schedule. `GI-STEWARD-0003` names `fyremael` as ordinary Human Steward and `jimsteeg` as recovery owner only. A GitHub approval or mechanical merge is not reserved authorization. Do not route routine work to the recovery owner unless an exact recovery operation names that role.

Historical T3 records retain their recorded predecessor rules. Prospective
non-reserved reviews use distinct logical passes, not mandatory distinct
sessions or executor identities. Destructive disposal and corpus admission
remain reserved and retain their declared authority gates. Re-read protected
INTELLECT authority before relying on this projection; drift fails closed.
