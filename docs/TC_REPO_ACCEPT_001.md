# TC-REPO-ACCEPT-001 — Dedicated-repository bootstrap acceptance

## Status

Prepared for exact-head T3 review. The destination remains inactive.

## Purpose

Accept the complete retained TROVE–CURATA bootstrap surface from `grandchallenge/INTELLECT` without creating a new bootstrap authority source.

The imported ladder is:

`extraction → PII observation → governed transformation → duplicate observation → quality-signal observation`.

It remains an observation-only retained/synthetic ladder. Import does not promote any observation into an admission, rejection, privacy, legality, rights, quality, contamination or training-fitness conclusion.

## Source binding

The acceptance package binds:

- INTELLECT issue 68 and source-closure PR 69;
- the exact source-closure candidate head and tree;
- the protected INTELLECT snapshot from which the inventory was generated;
- the exact source closure record, its file digest and canonical-record digest;
- all 92 source artifact identities;
- the source fixture, schema and workflow roots;
- the Fixture 003 and Fixture 004 review defects and prospective remedies.

The source PR is not yet protected. Consequently, destination acceptance remains pending even if this package passes review.

## Immutable import

Every source artifact appears under `bootstrap/intellect-snapshot/` at its original relative path. The destination validator recomputes Git blob identities directly from file bytes and requires exact equality with the source inventory.

The snapshot is historical evidence. It is not the destination's future source layout, and it may not be edited after acceptance. Future corrections are successor records, not history rewrites.

## Independent replay

Destination workflows install the pinned providers, set the imported source namespace as the Python execution root, replay Fixtures 001–005 and require two byte-identical Fixture 005 reports. This confirms operational continuity without making providers authoritative.

## Activation boundary

This package does not activate the repository. Activation requires both protected merges and a successor two-sided readback that binds:

- the INTELLECT source-closure protected merge;
- the destination-acceptance protected merge;
- the exact acceptance records and replay results on both protected branches.

Only then may substantive implementation begin with `TC-FIXTURE-006`.

## Nonclaims

No record is admitted, rejected, ranked, retained, deleted, suppressed or selected as canonical. No dataset-quality, privacy, legality, rights, contamination-absence, training-fitness, release, downstream-improvement, novelty, priority or commercial claim is authorized.
